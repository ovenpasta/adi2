<?xml version="1.0" encoding="UTF-8"?>
<adi>
  <link rel="stylesheet" href="examples/css/demo_block.css"/>

  <window title="Block Layout Reference" width="617px" height="617px">
    <box id="Root" class="root">
      <label text="Block Layout Reference" class="title"/>

      <!-- what a box does by default -->
      <box class="section">
        <label text="What a box does by default" class="caption"/>
        <label text="Block is what a box does when nothing asks otherwise: children stack down the content area in order, each spanning it for want of a width of its own. No rule on this box declares display."
               class="note"/>
        <box class="demo">
          <box class="bar"/>
          <box class="bar"/>
          <box class="bar"/>
        </box>
      </box>

      <!-- a declared width is honoured -->
      <box class="section">
        <label text="A declared width is honoured" class="caption"/>
        <label text="A child is laid out at the width it declares, in pixels or as a fraction of the container's content area. Declaring none spans that area instead, and max-width caps whichever of the two applies. What is left over stays to the right: block centring is margin: auto, which Adi has no value for."
               class="note"/>
        <label text="The last three are the two edges of the box. A margin sits outside the width, so it comes off an automatic one and only moves a declared one along. Padding sits inside it, so both of the bottom pair are the same box with the text starting further in."
               class="note"/>
        <box class="demo">
          <label text="no width" class="bar"/>
          <label text="width: 120px" class="bar w120"/>
          <label text="width: 45%" class="bar w45"/>
          <label text="max-width: 160px" class="bar wcap"/>
          <label text="margin-left: 48px" class="bar indent"/>
          <label text="same margin, width: 280px" class="bar w280 indent"/>
          <label text="same width, padding-left: 48px" class="bar w280 inset"/>
        </box>
      </box>

      <!-- a declared height is honoured -->
      <box class="section">
        <label text="A declared height is honoured" class="caption"/>
        <label text="The height a child declares is taken the same way its width is. A child that declares none gets its content's height, which for a box with nothing in it is the next section."
               class="note"/>
        <box class="demo">
          <label text="height: 20px" class="bar h20"/>
          <label text="height: 34px" class="bar h34"/>
          <label text="height: 48px" class="bar h48"/>
        </box>
      </box>

      <!-- a child with nothing in it takes no height -->
      <box class="section">
        <label text="A child with nothing in it takes no height" class="caption"/>
        <label text="The right-hand box carries two extra children between its bars, each with height: auto and nothing inside. An empty child measures nothing and is never stretched to fill, so the two boxes are indistinguishable."
               class="note"/>
        <box class="cases">
          <box class="case">
            <label text="two bars" class="case-label"/>
            <box class="demo">
              <box class="bar"/>
              <box class="bar"/>
            </box>
          </box>
          <box class="case">
            <label text="two bars, two empty children" class="case-label"/>
            <box class="demo">
              <box class="bar"/>
              <box class="ghost"/>
              <box class="ghost"/>
              <box class="bar"/>
            </box>
          </box>
        </box>
      </box>

      <!-- a percentage height -->
      <box class="section">
        <label text="A percentage height" class="caption"/>
        <label text="A percentage resolves against the container's content box, not against whatever height the child came in with, so it holds on every pass. These three take all of it, half of it and a quarter of it."
               class="note"/>
        <box class="cases">
          <box class="case">
            <label text="height: 100%" class="case-label"/>
            <box class="demo frame">
              <box class="bar fill"/>
            </box>
          </box>
          <box class="case">
            <label text="height: 50%" class="case-label"/>
            <box class="demo frame">
              <box class="bar half"/>
            </box>
          </box>
          <box class="case">
            <label text="height: 25%" class="case-label"/>
            <box class="demo frame">
              <box class="bar quarter"/>
            </box>
          </box>
        </box>
      </box>

      <!-- block and flex -->
      <box class="section">
        <label text="Block and flex" class="caption"/>
        <label text="The same three children, once under a box that declares nothing and once under display: flex. Both give each child the width it asks for; block stacks them down the content area while flex ranges them along a row and divides what is left over. demo_flex is the reference for what flex does from there."
               class="note"/>
        <box class="cases">
          <box class="case">
            <label text="no display" class="case-label"/>
            <box class="demo">
              <box class="chip chip-1"/>
              <box class="chip chip-2"/>
              <box class="chip chip-3"/>
            </box>
          </box>
          <box class="case">
            <label text="display: flex" class="case-label"/>
            <box class="demo flex-row">
              <box class="chip chip-1"/>
              <box class="chip chip-2"/>
              <box class="chip chip-3"/>
            </box>
          </box>
        </box>
      </box>
    </box>
  </window>
</adi>
